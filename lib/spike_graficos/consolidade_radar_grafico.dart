import 'package:flutter/material.dart';

class ConsolidadeRadarGrafico extends StatelessWidget {
  const ConsolidadeRadarGrafico({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Consolidade Gráfico"),
      ),
      body: SingleChildScrollView(
        child: Column(
          children: [
            
          ],
        ),
      ),
    );
  }
}