import 'package:flutter/material.dart';

class ItemMetricWidget extends StatelessWidget {
  const ItemMetricWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}