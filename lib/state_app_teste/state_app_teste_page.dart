import 'package:flutter/material.dart';

class StateAppTestePage extends StatelessWidget {
  const StateAppTestePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("State App Teste"),
      ),
    );
  }
}